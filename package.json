{
  "name": "tandem-matcher",
  "version": "2.0.0",
  "description": "Tandem-Matcher für Start with a Friend - Profile sammeln und matchen",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:webapp && npm run build:extension",
    "build:webapp": "vite build",
    "build:extension": "vite build --config vite.extension.config.ts && node scripts/copy-extension-static.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.25",
    "@types/chrome": "^0.0.268",
    "@types/leaflet": "^1.9.8",
    "typescript": "^5.4.2",
    "vite": "^5.1.6"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "xlsx": "^0.18.5"
  }
}
